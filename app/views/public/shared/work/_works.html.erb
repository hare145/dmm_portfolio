<div class="row justify-content-center">
  <% works.each do |work| %>
  <div class="col-2 pt-1 text-center">
    <%= link_to work.kind, softs_path( company: search_soft, work_id: work.id), class: "text-decoration-none" %>
  </div>
  <% end %>
</div>